node_modules/
dist/
.env

# Raw loose media in project root (not needed — src/assets/ is the source of truth)
*.pdf
*.png
Elderly_sage_*.jpeg
Sage_holding_palm_leaf_*.mp4
ChatGPT*

# But keep everything inside src/assets/ — that's what the app imports
!src/assets/
!src/assets/**

# OS
.DS_Store
Thumbs.db

.vercel
